{
  "name": "bible-app-source",
  "version": "1.0.0",
  "private": true,
  "description": "Modular source for the Bible App -- build.js bundles this into the single-file HTML output. verify.js checks the result.",
  "scripts": {
    "build": "node build.js",
    "verify": "node verify.js",
    "checkUnescaped": "node scripts/checkUnescapedHtmlInterpolation.js",
    "test": "node --test --test-force-exit",
    "check": "npm run build && npm run verify && npm run checkUnescaped && npm test"
  },
  "dependencies": {
    "esbuild": "^0.28.2"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
